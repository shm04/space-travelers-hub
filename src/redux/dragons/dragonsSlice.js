import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    num = 0
}
const dragonSlice = createSlice({
    name: "dragon",
    initialState,
    reducers: {},
    extraReducers: {
        
    }
})